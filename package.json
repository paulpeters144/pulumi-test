{
    "name": "pulumi-test",
    "main": "index.ts",
    "scripts": {
        "preview:uat": "cd config && pulumi preview --stack uat",
        "up:uat": "cd config && pulumi up --stack uat",
        "refresh:uat": "cd config && pulumi refresh --stack uat"
    },
    "devDependencies": {
        "@types/node": "^18",
        "prettier": "3.3.3",
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "@pulumi/azure-native": "^2.0.0",
        "@pulumi/pulumi": "^3.113.0",
        "@pulumi/random": "^4.16.3"
    }
}